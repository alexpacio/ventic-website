---
import Wordmark from "./Wordmark.astro";
import { content, type Lang } from "../i18n/content";
interface Props { lang: Lang; }
const { lang } = Astro.props;
const c = content[lang].adminPanelPage;
---
<section class="ahero">
  <div class="wrap inner">
    <div>
      <span class="brand"><Wordmark height={19} /></span>
      <span class="plate">{c.plate}</span>
      <h1 class="h2-big">{c.title[0]}<br />{c.title[1]}</h1>
    </div>
    <p class="body measure">{c.lead}</p>
  </div>
</section>

<style>
  .ahero { padding: 68px 0 44px; }
  .inner { display: flex; align-items: flex-end; justify-content: space-between; gap: 40px; }
  .brand { display: flex; align-items: center; margin-bottom: 26px; }
  .plate { margin-bottom: 22px; }
  .measure { max-width: 34ch; }
  @media (max-width: 900px) {
    .ahero { padding: 44px 0 32px; }
    .inner { flex-direction: column; align-items: flex-start; gap: 24px; }
  }
</style>
