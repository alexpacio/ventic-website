Ventic provides:

- inference host agent -> this runs in inferencing nodes
- overlay encrypted network for host reachability -> this runs in a public network whereas relay nodes could be private
- LGTM stack powered by Grafana -> this runs on your own docker infrastructure
- OpenAPI/Anthropic compatible LLM proxy -> this runs on your own docker infrastructure
- admin panel -> this runs on your own docker infrastructure
- Coding Agent wiring tool -> runs directly on desktops where coding agents are being used. this automatically configures your favourite coding agent

Optional:
(these run on your own docker infrastructure)
AI Agent:
- Deepseek harness
- Openclaw

RAG:
- OpenRAG
- QDrant
- Embedding models